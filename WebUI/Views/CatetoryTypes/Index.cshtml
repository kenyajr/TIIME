@model IEnumerable<Domain.Concrete.CatetoryType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.categoryTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.categoryTypeDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.createdDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.categoryTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.categoryTypeDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.createdDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.categoryTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.categoryTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.categoryTypeID })
        </td>
    </tr>
}

</table>
